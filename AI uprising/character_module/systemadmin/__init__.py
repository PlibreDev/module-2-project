from .systemadmin import SystemAdmin

# Personal note: the init file imports from the .py files in the same directory. In the program file the import comes from the module which needs to carry the same name as the directory.